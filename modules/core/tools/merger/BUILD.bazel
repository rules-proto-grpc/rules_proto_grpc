load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_clippy")

rust_binary(
    name = "merger",
    srcs = ["main.rs"],
    visibility = ["//visibility:public"],
)

rust_clippy(
    name = "merger_clippy",
    testonly = True,
    deps = [
        ":merger",
    ],
)
