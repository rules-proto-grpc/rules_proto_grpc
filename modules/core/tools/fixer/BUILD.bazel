load("@rules_rust//rust:defs.bzl", "rust_binary", "rust_clippy")

rust_binary(
    name = "fixer",
    srcs = ["main.rs"],
    visibility = ["//visibility:public"],
)

rust_clippy(
    name = "fixer_clippy",
    testonly = True,
    deps = [
        ":fixer",
    ],
)
